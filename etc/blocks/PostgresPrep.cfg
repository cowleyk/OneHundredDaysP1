{
    "exclude": true,
    "fields": [
        {
            "formula": "{{ datetime.datetime.fromtimestamp($Attr[0]).isoformat() }}",
            "title": "date"
        },
        {
            "formula": "{{ ($Attr[1] + $Attr[2] + $Attr[3] + $Attr[4]) / 4 }}",
            "title": "price"
        },
        {
            "formula": "ETH-USD",
            "title": "currency"
        },
        {
            "formula": "{{ $Attr[5] }}",
            "title": "volume"
        }
    ],
    "log_level": "NOTSET",
    "name": "PostgresPrep",
    "type": "Modifier",
    "version": "1.0.0"
}